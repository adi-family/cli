# =============================================================================
# ADI Local Development Environment Configuration
# =============================================================================
# Copy this file to .env.local and customize as needed:
#   cp .env.local.example .env.local
#
# Then start services:
#   docker compose -f docker-compose.local.yml up -d
# =============================================================================

# -----------------------------------------------------------------------------
# General Settings
# -----------------------------------------------------------------------------
RUST_LOG=info

# -----------------------------------------------------------------------------
# Signaling Server (WebSocket relay)
# -----------------------------------------------------------------------------
# Salt for HMAC device ID derivation (persistent sessions)
# IMPORTANT: Change this in production!
HMAC_SALT=local-dev-salt-change-in-production

# -----------------------------------------------------------------------------
# Auth Service
# -----------------------------------------------------------------------------
# JWT secret for token signing (min 32 characters)
# IMPORTANT: Change this in production!
JWT_SECRET=local-dev-jwt-secret-32-chars-minimum-here

# Token expiry in hours (default: 168 = 7 days)
JWT_EXPIRY_HOURS=168

# -----------------------------------------------------------------------------
# SMTP Configuration (optional for local dev)
# -----------------------------------------------------------------------------
# Without SMTP, email verification won't work, but you can still test the API
# For local testing, use a service like Mailhog, Mailpit, or Ethereal Email

# Mailpit (recommended for local dev):
#   docker run -d -p 1025:1025 -p 8025:8025 axllent/mailpit
#   Then set: SMTP_HOST=localhost, SMTP_PORT=1025
#   View emails at: http://localhost:8025

# Ethereal Email (free fake SMTP):
#   https://ethereal.email - create account and use credentials

SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=noreply@localhost
SMTP_FROM_NAME=ADI Auth (Local)

# -----------------------------------------------------------------------------
# Web UI
# -----------------------------------------------------------------------------
# These are baked into the Next.js build, but shown here for reference
# NEXT_PUBLIC_API_URL=http://localhost:8090
# NEXT_PUBLIC_WS_URL=ws://localhost:8080/ws

# -----------------------------------------------------------------------------
# Cocoon Worker
# -----------------------------------------------------------------------------
# Secret for persistent device ID (optional - auto-generated if not set)
# Generate with: openssl rand -base64 36
# COCOON_SECRET=

# -----------------------------------------------------------------------------
# Plugin Registry (optional)
# -----------------------------------------------------------------------------
# REGISTRY_DATA_PATH=/data
