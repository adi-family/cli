# Dangerously publish selected plugins to local plugin server
# No prompts — edit the plugin list in the .sh file, then fire and forget

[workflow]
name = "dangerously-publish-selected-to-local-plugin-server"
description = "Build and publish hardcoded plugins to local registry (no lint, no prompts)"

[[steps]]
name = "Publish plugins to local registry"
run = """
"$WORKFLOWS_DIR/dangerously-publish-selected-to-local-plugin-server.sh"
"""
