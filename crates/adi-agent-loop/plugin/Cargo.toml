[package]
name = "adi-agent-loop-plugin"
version = "0.8.8"
edition = "2021"
license = "BSL-1.0"
description = "ADI Agent Loop plugin providing CLI commands for autonomous LLM agents"
authors = ["ADI Team"]

[lib]
crate-type = ["cdylib"]

[dependencies]
adi-agent-loop-core = { path = "../core" }
lib-plugin-abi = { path = "../../lib-plugin-abi" }
abi_stable = "0.11"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tokio = { version = "1.49", features = ["full"] }
async-trait = "0.1"
anyhow = "1.0"
console = "0.15"
dialoguer = "0.11"