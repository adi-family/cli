[plugin]
id = "adi.agent-loop"
name = "ADI Agent Loop"
version = "0.8.8"
type = "core"
author = "ADI Team"
description = "Autonomous LLM agent with tool execution and session management"
min_host_version = "0.8.0"

[cli]
command = "agent-loop"
description = "Autonomous LLM agents for code tasks"
aliases = ["al"]

[[provides]]
id = "adi.agent-loop.cli"
version = "1.0.0"
description = "CLI commands for agent loop"

[tags]
categories = ["agent", "llm", "automation", "workflow"]

[binary]
name = "adi_agent_loop_plugin"
