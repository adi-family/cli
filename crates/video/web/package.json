{
  "name": "@adi-family/plugin-video",
  "version": "0.1.0",
  "type": "module",
  "main": "src/types.ts",
  "types": "src/types.ts",
  "exports": {
    ".": "./src/types.ts",
    "./build": "./src/index.ts"
  },
  "scripts": {
    "generate": "cargo check -p video-plugin",
    "build": "bun run generate && bun build src/index.ts --outdir ../../../dist/adi.video --entry-naming 'web.[ext]' --format esm --minify --external '@adi-family/*'",
    "typecheck": "tsc --noEmit",
    "dev": "bun build src/index.ts --outdir ../../../dist/adi.video --entry-naming 'web.[ext]' --format esm --minify --external '@adi-family/*' --watch"
  },
  "dependencies": {
    "@adi-family/plugin-router": "workspace:*",
    "@adi-family/cocoon-plugin-interface": "workspace:*",
    "@adi-family/sdk-plugin": "workspace:*",
    "@adi-family/video-sdk": "workspace:*",
    "lit": "catalog:"
  },
  "devDependencies": {
    "typescript": "catalog:"
  }
}
