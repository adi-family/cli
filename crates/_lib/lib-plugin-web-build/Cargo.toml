[package]
name = "lib-plugin-web-build"
version = "0.1.0"
edition = "2021"
license = "BSL-1.0"
description = "Build-time codegen for ADI web plugins — config, index, eventbus"

[dependencies]
lib-typespec-api = { path = "../../tsp-gen/core", default-features = false }
toml = "0.8"
anyhow = "1"
convert_case = "0.6"
