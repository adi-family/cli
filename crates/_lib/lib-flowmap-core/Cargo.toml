[package]
name = "lib-flowmap-core"
version = "0.1.0"
edition = "2024"
license = "BSL-1.1"
description = "Core types for FlowMap code visualization"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
