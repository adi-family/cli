[plugin]
id = "adi.embed"
name = "ADI Embed"
version = "0.1.0"
type = "core"
author = "ADI Team"
description = "Text embedding service using fastembed/ONNX for local ML inference"
min_host_version = "0.8.0"

[[provides]]
id = "adi.embed"
version = "1.0.0"

[binary]
name = "plugin"

[tags]
categories = ["embedding", "ml", "onnx", "fastembed"]
