[package]
name = "lib-signaling-protocol"
version = "0.1.0"
edition = "2021"
license = "BSL-1.0"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
serde_json = "1.0"
