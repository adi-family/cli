[package]
name = "linter-plugin"
version = "3.0.0"
edition = "2021"
license = "BSL-1.0"
description = "ADI Linter plugin providing CLI commands for code linting"
authors = ["ADI Team"]

[lib]
crate-type = ["cdylib"]

[dependencies]
# Plugin SDK - one import for everything
lib-plugin-prelude = { path = "../../lib/lib-plugin-prelude" }
linter-core = { path = "../core" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[package.metadata.plugin]
id = "adi.linter"
name = "ADI Linter"
type = "core"

[package.metadata.plugin.compatibility]
api_version = 3
min_host_version = "0.9.0"

[package.metadata.plugin.cli]
command = "lint"
description = "Lint code files in the current project"
aliases = ["l"]

[[package.metadata.plugin.provides]]
id = "adi.linter.cli"
version = "3.0.0"
description = "CLI commands for code linting"

[package.metadata.plugin.tags]
categories = ["linting", "code-quality", "static-analysis"]
