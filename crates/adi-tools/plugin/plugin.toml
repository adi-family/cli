[plugin]
id = "adi.tools"
name = "Tool Index"
version = "0.1.0"
type = "core"
author = "ADI Team"
description = "Searchable index of CLI tools for LLM agents"
min_host_version = "0.8.0"

[cli]
command = "tools"
description = "Search and discover CLI tools"
aliases = ["t"]

[[provides]]
id = "adi.tools.cli"
version = "1.0.0"
description = "CLI commands for tool discovery"

[tags]
categories = ["tools", "search", "llm", "agents"]

[binary]
name = "plugin"
