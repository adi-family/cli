[package]
name = "adi-coolify-http"
version = "0.8.4"
edition = "2021"
license = "BSL-1.0"
authors = ["ADI Team"]
description = "HTTP server for ADI Coolify - REST API for deployment management"

[[bin]]
name = "adi-coolify-http"
path = "src/main.rs"

[dependencies]
adi-coolify-core = { path = "../core" }
tokio = { version = "1.49", features = ["full"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
axum = "0.7"
tower = "0.4"
tower-http = { version = "0.5", features = ["cors", "trace"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
anyhow = "1.0"

# HTTP utilities
lib-http-common = { path = "../../lib/lib-http-common" }
