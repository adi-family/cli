{
  "name": "@adi-family/plugin-router",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/types.ts",
  "types": "src/types.ts",
  "exports": {
    ".": "./src/types.ts",
    "./build": "./src/index.ts"
  },
  "scripts": {
    "generate": "cargo check -p router-plugin",
    "build": "bun run generate && bun build src/index.ts --outdir ../../../../dist/adi.router --entry-naming 'web.[ext]' --format esm --minify --external '@adi-family/*'",
    "typecheck": "tsc --noEmit",
    "dev": "bun build src/index.ts --outdir ../../../../dist/adi.router --entry-naming 'web.[ext]' --format esm --minify --external '@adi-family/*' --watch"
  },
  "dependencies": {
    "@adi-family/plugin-command-palette": "workspace:*",
    "@adi-family/plugin-debug-screen": "workspace:*",
    "@adi-family/plugin-slots": "workspace:*",
    "@adi-family/sdk-plugin": "workspace:*",
    "lit": "catalog:"
  },
  "devDependencies": {
    "typescript": "catalog:"
  }
}
