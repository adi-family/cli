[package]
name = "lib-signaling-protocol"
version = "0.1.0"
edition = "2021"
license = "BSL-1.0"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[build-dependencies]
lib-typespec-api = { path = "../../../../crates/tsp-gen/core", default-features = false }

[dev-dependencies]
serde_json = "1.0"
