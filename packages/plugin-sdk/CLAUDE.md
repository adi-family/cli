plugin-sdk, build-required, local-package
- Entry point is `dist/` — always run `bun run build` after editing `src/` for changes to take effect in apps
- `bun run dev` for watch mode during active development
